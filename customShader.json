precision highp float;

uniform sampler2D sourceTexture;

varying vec2 vUV;

void main(void) {
    vec4 color = texture2D(sourceTexture, vUV);
    gl_FragColor = vec4(color.r, color.r, color.r, 1.0); // Use red channel for lightmap
}